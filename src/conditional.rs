// Conditional - Used to check the condition of something and act accordingly

pub fn run() {
    let age = 18;
    let check_id = false;

    // do ifelse
    if age >= 21 && check_id {
        println!("Bartender: What wpuld you like to drink?");
    } else {
        println!("Sorry you need to leave");
    }
}
